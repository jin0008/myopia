/**
 * 아직 공개하지 않은 기능을 화면에서만 가려두는 스위치.
 *
 * 코드를 지웠다가 되살리는 것보다 낫다 — 되살릴 때 무엇을 지웠는지 찾아야 하고,
 * 그 사이 다른 변경과 섞이면 그대로 되돌아오지 않는다.
 */

/**
 * myodoc 앱이 아직 정식 론칭 전이라, 병원 관리자에게 "myodoc 관리" 버튼을
 * 보이지 않게 둔다. 라우트(`/partner/profile`)는 살아 있어서 주소를 직접
 * 열면 동작한다 — 론칭 전 내부 확인에 필요하다.
 *
 * 론칭할 때 이 값 하나만 true로 바꾸면 된다.
 */
export const MYODOC_PORTAL_VISIBLE = false;
